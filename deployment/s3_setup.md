# Steps to create S3 bucket
# 🪣 S3 Setup Guide for SuperOps AI Analytics

This S3 bucket stores uploaded CSVs, reports, and insights generated by the backend.

## 1. Create the S3 Bucket

Run the following AWS CLI command:

```bash
aws s3api create-bucket \
  --bucket superops-ai-data \
  --region us-east-1 \
  --create-bucket-configuration LocationConstraint=us-east-1
